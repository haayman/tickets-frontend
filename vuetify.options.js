/**
 * @returns {import ("vuetify/types").Framework}
 */
export default function () {
  return {
    theme: {
      dark: true,
      themes: {
        dark: {
          primary: "#f0940a",
        },
      },
    },
  };
}
